name: modules.ops.snapshot
version: 1.4.0
engine_api: "^1.4"
actions:
  SNAPSHOT:
    modes: [SINGLE]
    input_schema: schema/in.json
    output_schema: schema/out.json
    required_scopes: []
    secrets: []
    resources: { rps: 10, burst: 20 }
