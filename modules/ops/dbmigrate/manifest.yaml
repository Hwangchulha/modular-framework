name: modules.ops.dbmigrate
version: 1.4.0
engine_api: "^1.4"
actions:
  MIGRATE:
    modes: [SINGLE]
    input_schema: schema/in.json
    output_schema: schema/out.json
    required_scopes: []
    secrets: []
    resources: { rps: 2, burst: 4 }
  CHECK:
    modes: [SINGLE]
    input_schema: schema/in.json
    output_schema: schema/out.json
    required_scopes: []
    secrets: []
    resources: { rps: 5, burst: 10 }
